# Copy this file to .env and fill values before deploying on Render
# Option A (recommended on Render): Provide the entire service account JSON as a single env var:
# Paste the raw JSON object as the value for SERVICE_ACCOUNT_JSON (must be valid JSON string).
SERVICE_ACCOUNT_JSON={"type":"service_account", ...}

# OR Option B: Upload the service account JSON file to the server and set GOOGLE_APPLICATION_CREDENTIALS path
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Protect admin endpoints (send/broadcast/list)
ADMIN_API_KEY=changeme_api_key

# (Optional) SQLite file path (defaults to ./tokens.db)
SQLITE_FILE=./tokens.db
PORT=3000
